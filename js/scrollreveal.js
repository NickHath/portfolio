window.sr = ScrollReveal();

var fadeOptions = {
  reset: false,
  distance: '80px',
  duration: 1400,
  // delay: 450,
  delay: 250,
  scale: 0,
  origin: 'bottom'
}

// sr.reveal('.experience', fadeOptions);
// sr.reveal('.subsection', fadeOptions);
sr.reveal('.fadein', fadeOptions);
